# MySQL (from database container)
MYSQL_URL=localhost
MYSQL_PORT=3306
MYSQL_DB=quality_defects
MYSQL_USER=root
MYSQL_PASSWORD=changeme

# Frontend origin for CORS (set to deployed frontend URL)
FRONTEND_ORIGIN=http://localhost:3000

# Toggle simple header-based security
APP_SECURITY_ENABLED=true
